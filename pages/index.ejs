<% if(product) { %>
    <div>
        <h1>
            <%=product.title %>
        </h1>
        <h1>
            <%=product.rating %>
        </h1>
        <p>
            <%=product.description %>
        </p>
        <img src="<%=product.thumbnail %>" alt="">
    </div>

<% } %>