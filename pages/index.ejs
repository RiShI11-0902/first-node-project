<style>
    div{
        border: 2px solid black;
        border-radius: 10px;
    }
</style>

<script>
async function deletep(product) {
    console.log(product);
    const response = await fetch('/products/'+product, {
        method: "DELETE"
    })
}
</script>
<h1>hello</h1>

<% if(product) { %>
<div >
    <h1><%=product.title %></h1>
    <img src="<%=product.thumbnail %>" alt="">
    <p><%=product.rating %></p>
    <p><%=product.description %></p>
    <button onclick="deletep('<%=product._id %>')">
    delete</button>
</div>
<% } %>